This is a sample application to integrate the qodo merge AI tool which automatically reads the PR and suggests the improvements and reviews the
code changes.
Using the OpenAI key to connect the github actions which triggers the PR reviews.